package fastSwiss.telegramBot

/**
 * This is one of the easiest bot - it will just print information about itself
 */
suspend fun main(vararg args: String) {
    val botToken = args.first()

//    val bot = telegramBot(botToken)
//    println(bot.getMe())
}
